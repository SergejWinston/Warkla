# Migrations

Flask-Migrate directory placeholder.

Initialize migrations locally:

1. flask --app run.py db init
2. flask --app run.py db migrate -m "initial schema"
3. flask --app run.py db upgrade
